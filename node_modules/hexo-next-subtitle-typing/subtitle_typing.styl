.site-subtitle {
  min-height: 1.2em; /* Keep minimum height to prevent page jump during typing */
  position: relative;
}

.site-subtitle::after {
  content: "|";
  position: relative;
  animation: blink 0.8s infinite;
}

@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0; }
}
